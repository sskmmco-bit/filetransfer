# Development / testing tooling — NOT installed into the production image.
#   pip install -r requirements-dev.txt
-r requirements.txt

# Load testing (see load_tests/locustfile.py + load_tests/README.md)
locust==2.32.4
